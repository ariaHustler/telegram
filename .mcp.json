{
  "mcpServers": {
    "telegram-local": {
      "command": "python",
      "args": ["${PLUGIN_ROOT}/scripts/telegram_mcp.py"],
      "env": {
        "TELEGRAM_PLUGIN_DATA_DIR": "${LOCALAPPDATA}/Codex/telegram-plugin"
      }
    }
  }
}
